--- a/Makefile.am
+++ b/Makefile.am
@@ -13,7 +13,7 @@ if BUILD_JNI_INTERFACE
 SUBDIRS += liblttng-ust-java liblttng-ust-jul
 endif
 
-SUBDIRS += tests doc
+SUBDIRS += tests
 
 #temporarily disabled
 # liblttng-ust-malloc
--- a/Makefile.in
+++ b/Makefile.in
@@ -137,7 +137,7 @@ DIST_SUBDIRS = . include snprintf librin
 	liblttng-ust liblttng-ust-ctl liblttng-ust-fork \
 	liblttng-ust-dl liblttng-ust-libc-wrapper \
 	liblttng-ust-cyg-profile tools liblttng-ust-java \
-	liblttng-ust-jul tests doc
+	liblttng-ust-jul tests
 DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
 distdir = $(PACKAGE)-$(VERSION)
 top_distdir = $(distdir)
@@ -321,7 +321,7 @@ ACLOCAL_AMFLAGS = -I config
 SUBDIRS = . include snprintf libringbuffer liblttng-ust-comm \
 	liblttng-ust liblttng-ust-ctl liblttng-ust-fork \
 	liblttng-ust-dl liblttng-ust-libc-wrapper \
-	liblttng-ust-cyg-profile tools $(am__append_1) tests doc
+	liblttng-ust-cyg-profile tools $(am__append_1) tests
 
 #temporarily disabled
 # liblttng-ust-malloc
