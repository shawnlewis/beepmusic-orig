#for dev_id, dev in self.dtab.iteritems():
#    self.logc('STOP %s' % dev_id)
#    dev.stop()

#for dev_id, dev in self.dtab.iteritems():
#    self.logc('START %s' % dev_id)
#    dev.start()

#for dev_id, dev in self.dtab.iteritems():
#    tests.core.device_check_base(dev_id, dev)

